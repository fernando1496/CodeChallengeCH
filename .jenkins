pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                bat "mvn compile"
                sh 'echo "Buildin automation framework"'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}
